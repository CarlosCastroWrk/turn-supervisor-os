// The live app root. The original wave-1 App component that used to live here
// was dead code (this default export has pointed at LaunchIntegratedApp since
// the launch-command-center takeover) and was removed in the Aug 2026 purge.
import { LaunchIntegratedApp } from './features/launch-command-center/LaunchIntegratedApp';

export default LaunchIntegratedApp;
